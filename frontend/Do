# Dev container for React + Vite
FROM node:20-alpine

WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci

COPY . .
# Expose Vite dev server
EXPOSE 3000

# Bind to all interfaces so it works inside Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]

